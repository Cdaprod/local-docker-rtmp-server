# NAS Configuration
NAS_IP_OR_HOSTNAME=192.168.0.19         # IP address or hostname of your NAS
NAS_SHARE_NAME=volume1/videos           # Share name/path on the NAS
NAS_USERNAME=                           # NAS username (leave blank to prompt if not pre-configured)
NAS_PASSWORD=                           # NAS password (leave blank to prompt if not pre-configured)

# Social Media Stream Variables
VIDEO_ID=                               # YouTube video ID for the live stream (e.g., "dQw4w9WgXcQ")
STREAM_KEY=                             # Facebook Live stream key
TWITCH_CHANNEL_NAME=cdaproductions      # Twitch channel name (replace with your Twitch channel)

# Twitch API Credentials (Optional if generating access token dynamically)
TWITCH_CLIENT_ID=                       # Twitch Client ID (required if generating tokens manually)
TWITCH_CLIENT_SECRET=                   # Twitch Client Secret (required if generating tokens manually)

# Deployment Environment
ENVIRONMENT=development                 # Set environment to "development", "staging", or "production"

# Additional Configuration (Optional)
TZ=UTC                                  # Timezone for the container (default: UTC)
NGINX_WORKER_PROCESSES=auto             # Nginx worker processes (default: auto)
NGINX_WORKER_CONNECTIONS=2048           # Nginx worker connections (default: 2048)
MAX_SEGMENT_DURATION=6                  # Maximum HLS segment duration in seconds

# Secure Notes
# - Do not share this file with sensitive values filled in.
# - Use Docker secrets or encrypted storage for production environments.