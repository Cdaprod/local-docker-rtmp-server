# Example environment for watchdog app

# Path to the main config file (default: /app/config.yaml)
CONFIG_PATH=/app/config.yaml

# Example: external API endpoint (used in enrich_api.py)
ENRICH_API_URL=http://enrich-api:8000/enrich/

# Example: set log level (implement support in main.py if desired)
LOG_LEVEL=INFO